console.log("JavaScript has loaded");

// properties (the data) of a ball
let colour = "orange";
const radius = 10;
let xVelocity = 5; // the speed of a ball on the x-axis
let yVelocity = 6; // the speed of a ball on the y-axis

// 1. fiddle around with the properties of the ball. Do change them! And refresh the page.

// creation of a new ball on the drawing canvas. 
createNewBall(radius, colour, xVelocity, yVelocity);

// 2. try to add a new ball using createNewBall function

//functionToRaise the speed or Velocity or Colour